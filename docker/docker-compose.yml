version: '3'
services:
  micro-frontends-component-poc:
    image: micro-frontends-component-poc:latest
    build:
      context: ../
      dockerfile: docker/Dockerfile
    env_file:
      - api.env
    network_mode: "host"
